<%= turbo_frame_tag "details" do %>
  <% if @articles.count > 0 %>
    <% @articles.each do |article| %>
      <%= render 'shared/article', article: article %>
    <% end %>
  <% else %>
  <%= render 'shared/empty', title: "#{@feed.title} has no articles" %>
  <% end %>
<% end %>
