<%= form_with(model: directory) do |form| %>
  <% if directory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(directory.errors.count, "error") %> prohibited this feed from being saved:</h2>

      <ul>
        <% feed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row mb-3">
    <%= form.label :title, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= form.text_field :title, class: "form-control" %>
    </div>
  </div>

  <div class="row mb-3">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
